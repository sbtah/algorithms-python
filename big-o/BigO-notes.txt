omega - best case scenario.
theta - average case.
omacron - worts case scenario.
--

Simplifications:
- Drop constant: O(2n) = O(n), O(n3) = O(n2) 
- Drop Non-dominants:

Different terms of inputs:



O(n) :
    number of n Operations,

O(n2):
    less efficient then O(n) from time coplexity point.